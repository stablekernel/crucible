# Build output
/bin/
bin/
/dist/
dist/

# Test & coverage artifacts
*.out
*.test
coverage*.out
coverage*.html

# Go workspace sums (kept local; not committed)
go.work.sum

# Fuzzing corpus seeds discovered at runtime
**/testdata/fuzz/

# Editor / OS noise
.DS_Store
*.swp

# Git worktrees
.worktrees/

# Docs site (Astro/Starlight) — isolated Node project under docs/.
# package-lock.json IS committed; build/cache/deps are not.
docs/node_modules/
docs/dist/
docs/.astro/
